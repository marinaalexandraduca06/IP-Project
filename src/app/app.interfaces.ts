export interface IColumnSchema {
  key: string;
  value: string;
  filter: string;
  sort: number;
}

export interface ButtonTypeInterface {
  name: string;
  primary: boolean;
  disabled: boolean;
}
